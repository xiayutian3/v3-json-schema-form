import { defineComponent } from 'vue'

export default defineComponent({
  name: 'NumberField',
  setup (props) {
    return () => {
      return (
        <div>number field</div>
      )
    }
  }
})
